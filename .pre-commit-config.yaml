repos:
  - repo: local
    hooks:
      - id: no-participant-data
        name: block participant data
        entry: python scripts/check_no_data.py
        language: system
        stages: [pre-commit]
